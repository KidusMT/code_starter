package com.pixel.app.utils;

import java.io.File;

public class SaveFileUtils {

    public static void savePrompt(File file) {
        // open dialog for showing the file name to be saved
        // check if file to exist locally before saving and getting crash if exists
        // save the file and shows a success toast message
    }
}
